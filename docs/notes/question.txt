Metadata nào bắt buộc cho mỗi chunk?
Hệ thống chia chunk dài quá 800 từ thì làm sao?
Model embedding đang dùng là model nào?
Làm sao để bot thông minh hơn?
Temperature của Gemini hiện tại là bao nhiêu?
Thời gian trung bình từ lúc hỏi đến khi nhận trả lời là bao lâu?
Khi upload file trùng tên thì hệ thống xử lý thế nào?
Chunk được gán những metadata nào?
Collection trong ChromaDB tên là gì?
Mẹo nào giúp bot đạt độ chính xác cao nhất?
    # if is_customer_support_intent(message):
    #     save_message("bot", "Đang kết nối với nhân viên CSKH...", session_id=session_id)
    #     result = await handle_cskh_transfer(message=message, customer_name="Bạn", force=True)
    #     return result





accesstoken:eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJleHAiOjE3NjU2NzgyNTIsInN1YiI6InBhcnRuZXIifQ.NusTFKlcbzk0e1zTG1IO0fYk_ifUVRS_IR-YH5V8JjM
sudo certbot --nginx -d chatbot.hachitechsolution.com



server {
    listen 80;
    server_name chatbot.hachitechsolution.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
